unit ArrayQuestions_u;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, Math;

type
  TForm1 = class(TForm)
    btnQuestion1: TButton;
    btnQuestion2: TButton;
    btnQuestion3: TButton;
    btnQuestion4: TButton;
    lbxAnswer: TListBox;
    procedure btnQuestion1Click(Sender: TObject);
    procedure btnQuestion2Click(Sender: TObject);
    procedure btnQuestion3Click(Sender: TObject);
    procedure btnQuestion4Click(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form1: TForm1;

implementation

{$R *.dfm}

procedure TForm1.btnQuestion1Click(Sender: TObject);
var

  line: String;
  j, k: Integer;
  arrMultiplication: Array [1 .. 6, 1 .. 6] of Integer;
begin
  // Create Times table
  for j := 1 to 6 do
  begin
    for k := 1 to 6 do
    begin
      arrMultiplication[j, k] := j * k;
    end;
  end;

  // Display
  lbxAnswer.clear;
  lbxAnswer.TabWidth := 20;
  line := '';

  for j := 1 to 6 do
  begin
    line := '';
    for k := 1 to 6 do
    begin
      line := line + inttostr(arrMultiplication[j, k]) + #9;
    end;
    lbxAnswer.Items.Add(line);
  end;

end;

procedure TForm1.btnQuestion2Click(Sender: TObject);
var

  j: Integer;
  k: Integer;
  line: String;
begin
  // create digits table


  
  // Display
  lbxAnswer.clear;
  lbxAnswer.TabWidth := 20;
  line := '';

end;

procedure TForm1.btnQuestion3Click(Sender: TObject);
var

  j: Integer;
  k: Integer;
  line: String;
  arrSum: array [1 .. 10, 1 .. 5] of Integer;
  iCount: Integer;
begin
  // create sum of indices table
  for j := 1 to 10 do
  begin
    for k := 1 to 5 do
    begin
      arrSum[j, k] := j + k;
    end;
  end;

  // Display
  lbxAnswer.clear;
  lbxAnswer.TabWidth := 20;
  line := '';

  for j := 1 to 10 do
  begin
    line := '';
    for k := 1 to 5 do
    begin
      line := line + inttostr(arrSum[j, k]) + #9;
    end;
    lbxAnswer.Items.Add(line);
  end;

end;

procedure TForm1.btnQuestion4Click(Sender: TObject);
var
  j: Integer;
  k: Integer;
  line: String;
  cChar: Char;
  arrChars: Array [1 .. 7, 1 .. 5] of Char;
begin
  // create lowercase character
  for j := 1 to 7 do
  begin
    for k := 1 to 5 do
    begin
      cChar := chr(Randomrange(97, 123));
      arrChars[j, k] := cChar;
    end;

  end;

  // Display
  lbxAnswer.clear;
  lbxAnswer.TabWidth := 20;
  line := '';

  for j := 1 to 7 do
  begin
    line := '';
    for k := 1 to 5 do
    begin
      line := line + arrChars[j, k] + #9;
    end;
    lbxAnswer.Items.Add(line);
  end;

end;

end.
